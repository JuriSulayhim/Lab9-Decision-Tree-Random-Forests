# Lab9-Decision-Tree-Random-Forests
This lab uses a loan dataset to predict whether a borrower will not fully pay back their loan using Decision Trees and Random Foreste.

## Results
The Random Forest model had higher overall accuracy, but it struggled to predict class 1 (`not.fully.paid`) because the dataset is imbalanced.
